name: Deploy para Azure Container Apps (Prod)

on:
  push:
    branches:
      - 'prod'

env:
  AZURE_RESOURCE_GROUP: ${{ secrets.AZURE_RESOURCE_GROUP }}
  AZURE_CONTAINER_REGISTRY: ${{ secrets.AZURE_CONTAINER_REGISTRY }}
  CONTAINER_APP_NAME: ${{ secrets.CONTAINER_APP_NAME }}
  DOCKER_IMAGE_NAME: ${{ secrets.DOCKER_IMAGE_NAME }}
  DOCKERFILE_PATH: Dockerfile
  IMAGE_TAG: ${{ github.sha }}

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    environment: Production

    steps:
    - name: Checkout do código
      uses: actions/checkout@v4

    # 1. 🔐 Login no Azure (Permissão de Contributor no Resource Group)
    - name: Login no Azure
      uses: azure/login@v2
      with:
        # Requer o GitHub Secret AZURE_CREDENTIALS (Service Principal JSON)
        creds: ${{ secrets.AZURE_CREDENTIALS }}
        enable-AzPSSession: true

    # 2. 🔑 Login no Azure Container Registry (ACR)
    # Usa as credenciais de Admin ativadas pelo Terraform.
    - name: Login no ACR
      uses: docker/login-action@v3
      with:
        registry: ${{ env.AZURE_CONTAINER_REGISTRY }}.azurecr.io
        username: ${{ secrets.ACR_USERNAME }} 
        password: ${{ secrets.ACR_PASSWORD }} 
    
    # 3. 🏗️ Build da Imagem Docker da API Go e Push para o ACR
    - name: Build e Push da imagem
      uses: docker/build-push-action@v5
      with:
        context: .
        push: true
        tags: ${{ env.AZURE_CONTAINER_REGISTRY }}.azurecr.io/${{ env.DOCKER_IMAGE_NAME }}:${{ env.IMAGE_TAG }}
        file: ${{ env.DOCKERFILE_PATH }}

    # 4. 🚀 Deploy usando Azure CLI (Linha única para evitar erro de sintaxe)
    - name: Deploy para Azure Container Apps
      uses: azure/cli@v2
      with:
        inlineScript: |
          az containerapp update --name ${{ env.CONTAINER_APP_NAME }} --resource-group ${{ env.AZURE_RESOURCE_GROUP }} --image ${{ env.AZURE_CONTAINER_REGISTRY }}.azurecr.io/${{ env.DOCKER_IMAGE_NAME }}:${{ env.IMAGE_TAG }} --secrets google-api-key=${{ secrets.GOOGLE_API_KEY }}
